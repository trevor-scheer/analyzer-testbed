pnpm-lock.yaml
apps/web/playwright-report/
apps/web/test-results/

# Design specs are faithful copies from the graphql-analyzer repo —
# don't reformat them so updates stay byte-comparable to the source.
docs/
